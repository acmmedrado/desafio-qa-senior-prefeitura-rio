[pytest]
addopts = -ra --strict-markers
testpaths = tests
markers =
    contract: response shape and API contract checks
    negative: invalid input, authorization, and error handling checks
    known_bug: tests that document current production-blocking defects
